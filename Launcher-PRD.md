# LanFlow 产品基线与发展需求

- 文档状态：当前产品基线
- 基线版本：v1.3.7（Git tag `v1.3.7`）
- 目标客户端：`native/LanFlow.Desktop`（WPF + .NET 8）
- 目标平台：Windows 10/11 x64
- 更新日期：2026-07-29

> 本文取代早期“类 Rolan + 类 Flow Launcher”的竞品拼接式需求。LanFlow 的后续工作以已发布的原生客户端、现有配置兼容性和可验证的用户价值为准；不再以 Tauri、Hain 或 Flow Launcher 的技术路线作为前置假设。

## 1. 产品定位

LanFlow 是一款本地优先的 Windows 轻量启动与整理工具。它帮助用户把常用程序、文件夹和快捷方式组织为可管理的分组，并以全局热键快速打开面板和启动目标。LanFlow 专注于用户已收纳的常用内容，不承担全电脑文件搜索、应用索引或通用命令面板职责。

核心承诺：

1. **本地可控**：用户数据保存在本机，不要求账户或云端服务；
2. **快速启动**：常用目标无需在开始菜单深层查找；
3. **可视化整理**：分组、排序、拖放和外观配置形成稳定工作区；
4. **安全导入**：外部工具或 AI 只能提交待审核的导入清单，不能直接改写用户配置；
5. **可持续升级**：全量包和轻量包均可通过应用内更新匹配正确通道。

## 2. 当前已发布能力

以下能力以 v1.3.7 已提交的原生客户端为准。

| 能力域 | 已提供能力 | 用户结果 |
| --- | --- | --- |
| 分组管理 | 创建、选择、重命名、删除分组；同名分组自动避免冲突 | 常用内容可按场景整理 |
| 启动项管理 | 添加、编辑、删除项目；支持 `.exe`、`.lnk`、文件和文件夹等 Shell 目标 | 常用目标可统一启动 |
| 拖放与排序 | 从资源管理器拖入；项目可在组内重排或拖到其他分组；支持手动与使用频次排序 | 减少重复整理成本 |
| 图标与交互 | Shell 图标提取、右键菜单、编辑模式、空状态与状态提示 | 项目更易识别和维护 |
| 面板内检索 | 输入时跨全部分组筛选已管理项目；空查询时保持当前分组 | 在已有工作区中快速定位项目 |
| 系统集成 | 可配置的全局热键、系统托盘、关闭隐藏、开机自启 | 不打断桌面工作流 |
| 外观设置 | 明暗/自定义主题、透明度、图标和卡片尺寸、间距、文本与布局选项 | 面板可适配个人使用习惯 |
| 更新与发布 | GitHub Release 检查、下载、替换和重启；区分 full/lite 更新通道 | 用户可持续获得匹配安装形态的更新 |
| 配置可靠性 | 本地 JSON 配置读取与临时文件原子替换保存 | 降低异常退出损坏配置的风险 |

## 3. 当前边界与非目标

以下事项不是当前已发布能力，也不得因文案或规划被误认为已经交付：

- 面向整个电脑的独立搜索窗口、开始菜单或用户目录应用索引、Everything 内置集成或文件索引；
- 网页搜索指令、计算器、系统命令、浏览器书签、剪贴板历史或通用命令面板；
- 自动识别和自动分类用户电脑中的全部软件；
- 内置大模型、静默 AI 操作、AI 直接修改 `config.json`；
- Flow Launcher/Hain 插件兼容、插件商店或开放 HTTP/WebSocket API；
- 跨平台客户端、Electron/Tauri 迁移或重写。

## 4. 产品对象与关键场景

| 用户场景 | 用户动作 | LanFlow 应提供的结果 |
| --- | --- | --- |
| 日常启动 | 按全局热键，选择或筛选已整理项目 | 快速显示面板并启动目标 |
| 工作区整理 | 新建分组，拖入或移动项目，调整顺序 | 常用目标按工作场景保持可见、可维护 |
| 外部查找后收纳 | 在 Everything、资源管理器或开始菜单中找到目标并拖入 LanFlow | 外部工具负责查找，LanFlow 只接收用户明确选择的目标 |
| 外部清单导入 | 选择 `import-manifest.json`，审查后确认 | 只合并选中的有效项目，不覆盖现有设置 |
| 升级 | 在设置页检查更新 | 下载与当前安装通道匹配的资产并完成重启更新 |
| 轻量部署 | 下载 lite 单文件 | 在具备 .NET 8 Windows Desktop Runtime 的机器上直接运行 |
| 离线部署 | 下载 full 压缩包并完整解压 | 无需预装 .NET 运行时即可运行 |

## 5. 后续需求的优先级

后续开发必须遵守“先保护已有数据和流程，再增加新入口”的顺序。详细里程碑见 `docs/DEVELOPMENT-ROADMAP.md`。

### P0：质量基线与安全批量导入

- 为 Core 建立自动化测试基线；
- 固化版本化 `import-manifest` 契约、示例与校验规则；
- 提供导入预览、逐项选择、重复/无效提示和一次性原子合并；
- 任何导入失败、取消或部分跳过都不能修改现有配置；
- 同步维护架构、发布与配置兼容文档。

**验收目标：** 外部工具无需理解内部 `AppConfig` 即可生成可审核清单；用户确认前内存和磁盘配置均不变化。

### P1：工作区快速定位

- 保留现有主面板和全局热键，不新增独立悬浮搜索窗口；
- 主面板激活后支持直接输入并进入搜索，无需先点击搜索框；
- 只搜索所有分组中的已管理项目，支持名称、路径、命令和可选别名的模糊匹配；
- 支持上下键选择、Enter 启动、Escape 清空、使用频次排序和明确的无结果状态；
- 跨分组结果显示所属分组，清空查询后恢复进入搜索前的工作区状态；
- 搜索匹配和排序规则位于 Core，不直接依赖 WPF 控件。

**验收目标：** 不扫描开始菜单、用户目录或全盘文件，也不依赖 Everything 等外部服务，用户仍可直接输入、定位并启动全部已管理项目；搜索不会改变现有分组、排序或配置。

### P2：工作区体验完善

详细实施方案见 `docs/P2-Workspace-Experience-Development-Plan.md`。本期按“先保护配置与已有流程，再改善可达性和反馈”的顺序交付：

- 补齐分组拖动排序、导航收起和网格 / 卡片视图的一致性交互；
- 让分组导航与项目区域在内容溢出时可滚动访问；
- 将现有设置、热键注册失败、启动异常、保存失败等反馈统一为可理解且可恢复的提示；
- 建立配置版本和受控迁移规则，保护旧用户的主题、布局、热键和项目数据；
- 在设置中明确配置文件地址，并提供不修改当前配置的完整备份能力。

**验收目标：** 新能力不破坏现有拖放、编辑、启动、热键、托盘和更新流程；旧配置可升级并保留用户设置，配置损坏或未来版本不会被空配置静默覆盖。

### P3：轻量性与启动性能

- 测量并优化冷启动、托盘驻留、热键呼出和首批图标显示；
- 保持外部查找工具、AI 和批量发现能力与主进程解耦，不增加常驻扫描器；
- 优化图标延迟加载、缓存和失效路径，避免后台工作阻塞主窗口；
- 继续控制 full/lite 两条发布通道的体积、依赖和升级可靠性。

**验收目标：** 性能结论来自目标机器实测；未使用搜索或导入时不启动额外索引任务，外部工具不可用不会影响 LanFlow 的启动、整理和已管理项目检索。

## 6. 数据与兼容性约束

- 当前配置位置：`%AppData%\LanFlow\config.json`；
- 配置中的 `groups`、`items` 与 `settings` 是用户数据，新增功能必须向后兼容；
- 外部输入必须使用独立的 `import-manifest`，不得要求外部工具构造内部 ID、图标对象、使用次数或设置字段；
- 配置写入继续使用临时文件后原子替换；
- 涉及路径的比较默认采用 Windows 不区分大小写语义，并在功能实现中明确路径规范化策略。

## 7. 发布形态

LanFlow 每个正式版本必须同时维护 **full** 与 **lite** 两个 Windows x64 发布通道。详细打包、命名、验证和 GitHub Release 规范见 `docs/ARCHITECTURE.md` 的“发布与更新规范”。

| 通道 | 交付形态 | 适用用户 |
| --- | --- | --- |
| full | 自包含的完整目录压缩包 | 需要免运行时部署、离线部署或最少前置条件的用户 |
| lite | 依赖 .NET 8 Windows Desktop Runtime 的单文件 `.exe` | 已具备运行时、希望最小下载和单文件运行的用户 |

## 8. 需求准入规则

新需求进入开发前，必须写清：

1. 解决的具体用户动作和可观察结果；
2. 是否读取、修改或迁移用户配置；
3. Windows 平台依赖、权限和失败降级策略；
4. 对 full/lite 两种发布形态的影响；
5. 自动化测试与手工回归范围；
6. 明确不包含的内容。

没有可验证验收标准的竞品功能对标，不进入排期。