<UserControl x:Class="LanFlow.Desktop.Controls.GroupNavigationControl"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LanFlow.Desktop.Controls"
             Background="Transparent">
    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="VerticalGroupsPanel">
            <VirtualizingStackPanel Orientation="Vertical" />
        </ItemsPanelTemplate>
        <ItemsPanelTemplate x:Key="HorizontalGroupsPanel">
            <VirtualizingStackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>

        <Style x:Key="GroupListStyle" TargetType="ListBox">
            <Setter Property="ItemsPanel" Value="{StaticResource VerticalGroupsPanel}" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding GroupLayout, ElementName=Root}" Value="top">
                    <Setter Property="ItemsPanel" Value="{StaticResource HorizontalGroupsPanel}" />
                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GroupItemStyle" TargetType="ListBoxItem">
            <Setter Property="Height" Value="{Binding GroupLabelSize, RelativeSource={RelativeSource AncestorType={x:Type local:GroupNavigationControl}}}" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="AllowDrop" Value="True" />
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="GroupItem_PreviewMouseLeftButtonDown" />
            <EventSetter Event="MouseEnter" Handler="GroupItem_MouseEnter" />
            <EventSetter Event="MouseLeave" Handler="GroupItem_MouseLeave" />
            <EventSetter Event="DragEnter" Handler="GroupItem_DragEnter" />
            <EventSetter Event="DragLeave" Handler="GroupItem_DragLeave" />
            <EventSetter Event="DragOver" Handler="GroupItem_DragOver" />
            <EventSetter Event="Drop" Handler="GroupItem_Drop" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Padding="12,0"
                                Background="{TemplateBinding Background}"
                                CornerRadius="7">
                            <TextBlock Text="{Binding Name}"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{Binding GroupLabelFontSize, RelativeSource={RelativeSource AncestorType={x:Type local:GroupNavigationControl}}}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Name}"
                                       AutomationProperties.Name="{Binding Name}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HoverBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                            </Trigger>
                            <DataTrigger Binding="{Binding GroupLayout, RelativeSource={RelativeSource AncestorType={x:Type local:GroupNavigationControl}}}" Value="top">
                                <Setter Property="Margin" Value="0,0,4,0" />
                                <Setter Property="MaxWidth" Value="180" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ListBox ItemsSource="{Binding ItemsSource, ElementName=Root}"
             SelectedItem="{Binding SelectedItem, ElementName=Root, Mode=TwoWay}"
             Style="{StaticResource GroupListStyle}"
             ItemContainerStyle="{StaticResource GroupItemStyle}"
             Background="Transparent"
             BorderThickness="0"
             Padding="0"
             HorizontalContentAlignment="Stretch"
             ScrollViewer.CanContentScroll="True"
             VirtualizingPanel.IsVirtualizing="True"
             VirtualizingPanel.VirtualizationMode="Recycling" />
</UserControl>
