<Window x:Class="LanFlow.Desktop.Views.EditGroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑分组"
        Width="390"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Border Padding="28" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="22" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="8" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="24" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock FontSize="20" FontWeight="SemiBold" Text="分组名称" />
                <TextBlock Margin="0,5,0,0" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Text="为你的启动项目创建清晰的分类。" />
            </StackPanel>
            <TextBlock Grid.Row="2" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Text="名称" />
            <TextBox x:Name="NameTextBox" Grid.Row="4" MinWidth="330" />
            <StackPanel Grid.Row="6" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button IsCancel="True" Content="取消" />
                <Button Margin="10,0,0,0" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="Confirm_Click" Content="保存" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
