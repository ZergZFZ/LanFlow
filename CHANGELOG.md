# LanFlow 变更日志

> 本文件记录 LanFlow Windows 客户端（`native/LanFlow.Desktop`）每个正式版本的变更。
> 版本号与 `native/LanFlow.Desktop/LanFlow.Desktop.csproj` 中的 `Version` / `AssemblyVersion` / `FileVersion` 保持一致。
> 发布闸门与资产命名规范见 `docs/ARCHITECTURE.md` §6。

## [1.5.1] - 2026-08-21

### 修复

- 修复开机自启后偶尔出现主窗口分页标签存在但内容为空的问题（此前需在常驻图标右键重启软件才能恢复）。
- 修复应用图标透明 / 加载不出来的问题：Shell 图标提取失败时增加短延迟重试，并仅缓存提取成功的图标，避免失败结果被长期缓存。

## [1.4.9] - 2026-08-13

### 新增：快捷键截图（第一批）

- 新增全局「截图」热键，默认 `Ctrl+Shift+A`，设置页可自定义；与全局热键冲突时阻止保存并提示。
- 触发后冻结全屏（支持多屏，含负坐标），拖拽框选选区，**松开自动复制选区到剪贴板**并退出。
- `Esc` 取消、不复制；防重入（连按只弹出一个框选窗口）。
- 主窗口可见时触发截图，画面不含 LanFlow 自身（截图前隐藏、结束后恢复）。

### 新增：截图编辑模式

- 框选后直接进入编辑，无需二次确认；绿色虚线选区框 + 8 个拖拽手柄可随时二次调整选区。
- 标注工具：箭头 / 矩形 / 颜色（8 色）/ 马赛克；支持撤销；支持另存为（会话内记忆保存目录）。
- 调整选区时保留并平移已有标注（相对原图锚定，不与内容脱节）。
- 标注边界四方向一致限制在选区内；修复分层窗口下保存图右/下留白（VisualBrush 合成）。

### 调整

- 全局默认热键 `Ctrl+Alt+Space` → `Ctrl+Alt+L`，避免与系统 / 输入法冲突（共享 Core 默认值）。

### 文档

- 活跃文档头部统一标注状态与最近更新日期；归档历史文档并新增 `docs/README.md` 索引。

## 说明

- 更早版本（≤ 1.4.8）的变更记录分散在 git 提交历史与 GitHub Release 中，暂未回溯补齐。
- 从 1.4.9 起，每次正式发布均需同步更新本文件。
