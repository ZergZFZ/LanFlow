1. 布局与项目，取消列表方式排列。点拉片方式排列后图标全部消失了。
2. 设置页面风格与配色没有和主页一致
3. 设置页面左侧分栏选中卡片后蓝色轮廓不全，被右边的分割线吞掉了一部分。
4. 布局与项目中 项目间距 改为 列间距，与行间距对应，当前的项目间距有问题。
5. 点开设置时，设置页面会阻挡主页面，尽量做成：点开设置页面，主页面与设置页面并排在屏幕上，但并不锁定，方便调试。
6. 透明度与材质中，透明度滑块后现实的百分比不全 只有1位数字   85  % 显示的8  %
7. 性能与缓存中，加入配置文件默认地址，如果能更换就更好了。
8. 分组标签-悬停 选项加一个自定义反应毫秒数，当前200毫秒显得有点卡。