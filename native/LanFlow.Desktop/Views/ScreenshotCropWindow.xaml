<Window x:Class="LanFlow.Desktop.Views.ScreenshotCropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="截图"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        Focusable="True"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid x:Name="RootGrid" Cursor="Cross">
        <Image x:Name="FrozenImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top" />
        <Canvas x:Name="OverlayCanvas" HorizontalAlignment="Left" VerticalAlignment="Top" />
        <Border HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,16,0,0"
                Background="#B0000000" CornerRadius="4" Padding="16,6">
            <TextBlock Text="拖动框选截图区域　Esc 取消" Foreground="White" FontSize="13" />
        </Border>
    </Grid>
</Window>
